namespace Tcp_Lib
{
    public enum Signals
    {
        START,
        STOP,
        RELOAD,
        ACCEPT,
        DENY,
        WAIT,
        RUNNING,
        CONNECTED,
        DISCONNECTED
    }
}